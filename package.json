{
  "name": "annuity-iq",
  "version": "2.0.0",
  "type": "module",
  "description": "AnnuityIQ - Professional Annuity Research & Comparison Tool",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "seed": "node scripts/seed-supabase.js",
    "build:managed": "python3 scripts/build_managed_accounts.py",
    "validate:data": "node scripts/validate-data.mjs",
    "test": "node --test",
    "smoke": "node scripts/smoke.mjs",
    "check": "npm run validate:data && npm test && npm run smoke && npm run build"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.1"
  },
  "devDependencies": {
    "puppeteer": "^24.37.5",
    "vite": "^6.3.5"
  }
}
